{
  "name": "bolao2026",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace client",
    "build:client": "npm run build --workspace client",
    "preview:client": "npm run preview --workspace client",
    "dev:server": "npm run dev --workspace server",
    "start:server": "npm run start --workspace server",
    "migrate:server": "npm run migrate --workspace server",
    "seed:server": "npm run seed --workspace server",
    "export:competition-data": "node ./server/db/export-competition-tables.js",
    "start": "npm run start:server"
  }
}
